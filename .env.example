# ─── VoteCircle Production Environment ─────────────────────
# Copy this file to .env.local for local dev, or set these
# as environment variables in Railway.

# JWT secret for auth (generate with: openssl rand -base64 32)
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# PostgreSQL connection string
# Railway auto-provides this when you add a Postgres database
DATABASE_URL=postgresql://user:password@localhost:5432/votecircle

# Node environment
NODE_ENV=production

# Port (Railway sets this automatically)
PORT=3000

# Data directory for voter files (Railway volume mount point)
DATA_DIR=/app/data
